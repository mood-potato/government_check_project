# 국회 발언 분석

유권자가 국회의원의 발언과 판단 패턴을 원문 근거와 함께 확인하는 서비스다.

## Language

**상반 발언 후보**:
동일 인물의 서로 다른 시점 발언 중 입장 차이가 있을 가능성이 있어 자동 분석으로 비교 대상으로 선정된 한 쌍이다. 확정적인 모순 판정은 아니다.
_Avoid_: 모순, 거짓말

**원문**:
발언의 날짜와 회의 맥락을 확인할 수 있는 국회 회의록의 근거 문서다.
_Avoid_: 증거

**최신 데이터**:
가장 최근의 일일 수집이 성공하여 서비스에 반영된 국회 자료다. 원천 공개 후 최대 24시간의 지연을 허용한다.
_Avoid_: 실시간 데이터

**홈 최신 사례**:
자동 분석을 통과한 상반 발언 후보 중 최근 발언 날짜가 가장 늦은 한 사례다. 날짜가 같으면 분석 점수가 더 높은 후보를 고른다.
_Avoid_: 최고 점수 후보, 최신 발언

## Language — AI 개발 프로세스

**AI 개발 프로세스**:
`.claude/skills/dev-process-orchestrator`가 조율하는 7단계(플로우 생성 → 플로우 검토 → 피쳐 기획 생성 → 피쳐 기획 검토 → 설계 → 개발 → 검증) 작업 흐름. `.claude/agents`와 `.claude/skills`는 git에 커밋되어 있어 clone만 하면 팀원도 동일하게 사용할 수 있다.
_Avoid_: 워크플로우, 스킬 시스템

**산출물 (Artifact)**:
AI 개발 프로세스의 각 단계가 만들어내는 결과 파일. `_workspace/`에 단계 번호를 붙여 저장한다 (`01_flow_create.md` ~ `07_review_log.md`).
_Avoid_: 결과물, 출력

**_workspace/**:
현재 진행 중인 기능의 산출물을 담는 디렉토리. 로컬 스크래치가 아니라 git에 커밋한다 — Notion 계정이 없는 팀원도 저장소만 clone하면 진행 중인 설계 문서를 볼 수 있어야 하기 때문이다 ([ADR-0001](./docs/adr/0001-commit-workspace-artifacts.md)). 새 기능을 시작하면 기존 `_workspace/`는 `_workspace_prev/`로 이동한다.
_Avoid_: 임시 폴더, 스크래치 디렉토리
